

# Core dependencies for Flask app
flask
toml
tomli
docker
# LLM and API dependencies
pydantic==2.10.6
openai==1.66.3
tenacity==9.0.0
tiktoken==0.9.0
boto3
baidusearch

# File handling and HTTP
aiofiles~=24.1.0
httpx>=0.27.0

# Logging
loguru==0.7.3

# Optional: Uncomment only if you need these specific featurespip install -r requirements.txt
# Browser automation (uncomment if needed)
# playwright~=1.51.0
browser-use==0.1.40

# Search engines (uncomment if needed)
googlesearch-python==1.3.0
duckduckgo_search==7.5.3

# MCP protocol (uncomment if needed)
mcp==1.5.0

# Development/Testing (uncomment for development)
# pytest~=8.3.5
# pytest-asyncio~=0.25.
colorama==0.4.6

# FastAPI (uncomment if switching from Flask)
fastapi==0.115.11
uvicorn==0.34.0
pyyaml==6.0.2
